import { FontAwesomeIcon } from '@fortawesome/react-fontawesome';
import styled from 'styled-components';

const StyledPromiseTickIcon = styled(FontAwesomeIcon)`
  color: #3273dc;
  margin-right: 10px;
`;

export { StyledPromiseTickIcon };
